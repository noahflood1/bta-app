specVersion: "v1"
serviceId: "bta-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "bta-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
